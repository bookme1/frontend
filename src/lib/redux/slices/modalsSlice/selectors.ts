import type { ReduxState } from "@/lib/redux";
import { bookApi } from "../../features/book/bookApi";

// export const selectBooks= (state: ReduxState) => state.book.books;
// export const selectFilter= (state: ReduxState) => state.book.filterOptions;
// export const selectFavorite= (state: ReduxState) => state.book.favorite;

// export const selectB= (state: ReduxState) => state[bookApi.reducerPath].queries.getBooks?.data;





